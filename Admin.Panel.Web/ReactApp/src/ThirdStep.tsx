const ThirdStep = () =>{
    return(
        <div>
            <DraggableComponent/>
            <div>
                <IconButton color="primary" aria-label="add" className="mt-50 mb-50 ml-50">
                    <Icon>add</Icon>
                </IconButton>
            </div>
        </div>
    );
}
