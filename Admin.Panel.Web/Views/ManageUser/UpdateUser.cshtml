@using Admin.Panel.Core.Entities
@model Admin.Panel.Core.Entities.UpdateUserViewModel

@{
    ViewData["Title"] = "Редактирование";
}

<h2>@ViewBag.Title</h2>

<form asp-route-returnUrl="@ViewData["ReturnUrl"]" method="post" asp-antiforgery="true">
    <hr />
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <div class="col-md-10">
            <label asp-for="Email"></label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <label asp-for="Nickname"></label>
            <input asp-for="Nickname" class="form-control" />
            <span asp-validation-for="Nickname" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <label asp-for="Email"></label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <label asp-for="IsUsed"></label>
            <input asp-for="IsUsed" class="form-control" />
            <span asp-validation-for="IsUsed" class="text-danger"></span>
        </div>
    </div>
    @*<div class="form-group">
        <div class="col-md-10">
            @{
                List<SelectListItem> lst1 = new List<SelectListItem>();
                for (int i = Model.ApplicationCompanies.Count - 1; i >= 0; i--)
                {
                    lst1.Add(new SelectListItem { Text = Model.ApplicationCompanies[i].CompanyName, Value = Model.ApplicationCompanies[i].CompanyId.ToString() });
                }
            }
            <label asp-for="CompanyId"></label>
            <select asp-for="CompanyId" asp-items="lst1" class="custom-select dropdown-stile">
                <option selected="selected" disabled="disabled"></option>
            </select>
            <span asp-validation-for="CompanyId" class="text-danger"></span>
        </div>
    </div>*@
    <button type="submit" class="btn btn-default">Создать</button>
</form>



@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}